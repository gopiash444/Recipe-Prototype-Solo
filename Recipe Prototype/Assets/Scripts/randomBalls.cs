using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomBalls : MonoBehaviour
{
    // spawn 4 balls 
    // determine 6 possible ball colors
    // randomize colors of the 4 balls from a list of 6 colors
    // determine the correct color/order for the balls to be selected from the colors present. Order is randomized
    // Begin while loop for 120 seconds
    // Player must click on the balls. Player has infinite chances to guess the right order until the timer runs out
    // If player guesses wrong, colors are re-randomized
    // Else player wins

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
